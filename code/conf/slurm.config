/* SLURM configuration.

This configuration file specifies SLURM executor settings to run on a cluster.
*/

executor {
    name = 'slurm'
    queueSize = 100
    pollInterval = '30 sec'
    submitRateLimit = '10sec'
}